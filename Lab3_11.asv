% matrix_obiekt3D.m
clear all; close all;


load('babia_gora.dat');

x = babia_gora(:,1); y = babia_gora(:,2); z = babia_gora(:,3);


plot3( x, y, z, 'b-' );


[rowNum, colNum] = size(babia_gora);
newMatrix = [babia_gora;ones()]

XR = Rz * Ry * Rx * babia_gora', % 3 rotacje po kolei
hold on;

figure; grid; plot3( XR(1,:), XR(2,:), XR(3,:), 'g-' ); % wynik obrotu

hold off;
